pipeline {
	agent none
  stages {
  	stage('Maven Install') {
    	agent {
      	docker {
        	image 'maven'
        }
      }
      steps {
      	sh 'mvn clean install'
      }
    }
    stage('Docker Build') {
    	agent any
      steps {
	      dir('Claims API/claims'){     
      	sh 'docker build -t apurva09/claims-docker.jar .'
        }
      }      
    }
  }
}
